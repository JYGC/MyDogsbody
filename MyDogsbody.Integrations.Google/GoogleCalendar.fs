module MyDogsbody.Integrations.Google.GoogleCalendar

// A placeholder until change #6 (google-account-integration) makes the calendar side real.
// The module name is corrected here from the old Infrastructure-prefixed one - this is the
// change that first makes this project real, so it is the change that fixes the last of the
// old naming.
let createGoogleCalendarHandler name =
    printfn "Hello %s" name
